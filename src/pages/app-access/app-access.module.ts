import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AppAccessPage } from './app-access';

@NgModule({
  declarations: [
    AppAccessPage,
  ],
  imports: [
    IonicPageModule.forChild(AppAccessPage),
  ],
})
export class AppAccessPageModule {}
